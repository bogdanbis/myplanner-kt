dburl: jdbc:postgresql://localhost:5432/myplanner_kt
dbusername: webadmin
dbpassword: webadmin
dbdialect: org.hibernate.dialect.PostgreSQLDialect
jwtSecret: Y2FyYW1iYV9zb21icmVybzIzOTQ4

server.port: 5432

spring:
    datasource:
        url: ${dburl}
        username: ${dbusername}
        password: ${dbpassword}
    jpa:
        database-platform: ${dbdialect}
        hibernate:
            ddl-auto: update
        show-sql: true
    servlet:
        multipart:
            max-file-size: 10MB
security:
    jwt:
        secret: ${jwtSecret}
        lifespanMs: 2_419_200_000 # 28 days
